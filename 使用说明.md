# FC内部AI生图工作台 - 使用说明

## 快速开始（完整版）

### 1. 解压文件
将 `FC-AI生图工作台-v1.0.0-完整版.zip` 解压到任意目录，例如：
```
D:\FC-AI生图工作台\
```

### 2. 启动程序
双击 `一键启动.bat` 文件，程序会自动：
- 启动本地开发服务器
- 自动打开浏览器访问 http://localhost:5175

### 3. 配置API
首次使用需要配置API：
1. 点击页面顶部的"API配置"按钮
2. 输入你的API地址和密钥
3. 点击"加载模型"，等待模型列表加载
4. 搜索并选择需要的模型，点击"确定启用"
5. 配置完成后即可开始生图

## 源码版使用方法（需要Node.js环境）

### 1. 安装Node.js
如果电脑没有安装Node.js，请先下载安装：
- 下载地址：https://nodejs.org/
- 推荐版本：LTS 18.x 或更高

### 2. 解压源码
将 `FC-AI生图工作台-v1.0.0-源码版.zip` 解压到任意目录

### 3. 安装依赖
在项目目录下打开命令行，运行：
```bash
npm install
```

### 4. 启动程序
双击 `一键启动.bat`，或运行：
```bash
npm run dev
```

## 常见问题

### Q: 启动后浏览器没有自动打开？
A: 手动在浏览器地址栏输入 http://localhost:5175

### Q: 端口5175被占用怎么办？
A: 修改 `vite.config.ts` 中的端口号，或关闭占用端口的程序

### Q: API配置后无法保存？
A: API配置保存在浏览器本地存储（localStorage），清除浏览器数据会丢失配置

### Q: 生成失败怎么办？
A: 
1. 检查API地址和密钥是否正确
2. 检查模型是否已确定启用
3. 按F12打开控制台查看错误信息
4. 尝试降低分辨率或质量

### Q: 如何更新到新版本？
A: 备份你的API配置（可以截图保存），然后替换整个项目文件夹

## 文件说明

```
FC-AI生图工作台/
├── src/                    # 源代码目录
│   ├── components/         # 组件
│   ├── context/            # 状态管理
│   └── ...
├── node_modules/           # 依赖包（完整版已包含）
├── mock-packages/          # 模拟包
├── index.html              # 入口HTML
├── package.json            # 项目配置
├── vite.config.ts          # Vite配置
├── tailwind.config.js      # Tailwind配置
├── 一键启动.bat            # 启动脚本
├── 版本说明.md             # 版本说明
└── 使用说明.md             # 本文档
```

## 技术支持
如有问题，请联系开发者。
